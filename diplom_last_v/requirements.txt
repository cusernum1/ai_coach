# ============================================================
# requirements.txt — Зависимости проекта AI-Coach (v3)
# ============================================================
# Проект = Telegram-бот + Web Mini-App + PostgreSQL + планировщик
# ============================================================

# ── LLM провайдеры ────────────────────────────────────────────
groq>=0.4.0
openai>=1.12.0

# ── Telegram-бот ─────────────────────────────────────────────
aiogram>=3.4.0            # асинхронный фреймворк для Telegram Bot API
aiohttp>=3.9.0            # http-клиент для aiogram + внешних API

# ── Веб-приложение (дашборд тренера + OAuth callback'и) ──────
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
jinja2>=3.1.3             # шаблоны HTML для веб-мини-приложения
httpx>=0.27.0             # асинхронный http (Strava, YooKassa)

# ── База данных (PostgreSQL) ──────────────────────────────────
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0           # драйвер PostgreSQL для SQLAlchemy (async)
aiosqlite>=0.19.0         # драйвер SQLite (async) — dev-режим SQLITE_FALLBACK=1
alembic>=1.13.0           # миграции (опционально — для продакшна)

# ── Планировщик задач ────────────────────────────────────────
apscheduler>=3.10.4       # периодические опросы, напоминания

# ── Вспомогательные ──────────────────────────────────────────
python-dotenv>=1.0.0
pydantic>=2.0.0
pydantic-settings>=2.2.0  # типизированный AppConfig из .env
loguru>=0.7.0

# ── Визуализация (для веб-дашборда) ──────────────────────────
plotly>=5.18.0
pandas>=2.2.0

# ── PDF-экспорт (из старого ядра) ────────────────────────────
fpdf2>=2.7.9

# ── Тестирование ─────────────────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-mock>=3.12.0
